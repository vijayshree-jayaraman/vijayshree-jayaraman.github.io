# Jekyll Starter (Light, Garamond)

Replace the site content with your own. To run locally:

1. Install Ruby and Bundler.
2. `bundle install`
3. `bundle exec jekyll serve`

Then open http://127.0.0.1:4000
